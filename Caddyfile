# Auto-HTTPS reverse proxy for FlowTech Hub.
# Caddy provisions + renews a Let's Encrypt certificate automatically once
# hub.flowtechapps.com's DNS points at this server and ports 80/443 are open.
hub.flowtechapps.com {
	encode zstd gzip
	reverse_proxy hub:4000
}
