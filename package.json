{
  "name": "flowtech-hub",
  "version": "0.1.0",
  "private": true,
  "description": "FlowTech Hub — internal company intranet for FlowTech Apps (BFF + React SPA on Microsoft 365).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev -w @flowtech/api",
    "dev:web": "npm run dev -w @flowtech/web",
    "build": "npm run build -w @flowtech/shared && npm run build -w @flowtech/api && npm run build -w @flowtech/web",
    "build:shared": "npm run build -w @flowtech/shared",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write .",
    "test": "npm run test -w @flowtech/web --if-present && npm run test -w @flowtech/api --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.19.1",
    "@typescript-eslint/parser": "^8.19.1",
    "concurrently": "^9.1.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
